<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin yang diperlukan oleh aplikasi -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GM2GPTCopy10"
        tools:targetApi="31">

        <!-- Aktivitas utama aplikasi -->
        <activity
            android:name="rrn.mobile.TodoTide.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Aktivitas untuk mengedit data -->
        <activity
            android:name="rrn.mobile.TodoTide.MapsEdit"
            android:exported="false" />

        <!-- Aktivitas tambahan (Maps) -->
        <activity
            android:name="rrn.mobile.TodoTide.Maps"
            android:exported="false" />

        <!-- Aktivitas untuk membuat item baru -->
        <activity
            android:name="rrn.mobile.TodoTide.CreateActivity"
            android:exported="true" />
    </application>
</manifest>
